# dependencies
node_modules
.venv/

# build output
.next
out
.vercel

# python
__pycache__/
*.pyc

# env & local
.env*.local
.DS_Store
*.tsbuildinfo

# large binary source data (재수집 가능, manifest로 관리)
data/pdfs/

# local caches & session artifacts
cache/
artifacts/
.omc/

# backtest stage caches (deterministic; keyed by dataset fingerprint, --retune to rebuild)
data/optuna_cache.json
data/spo_cache.pkl
data/s_weights_cache.pkl

# session/orchestration state
.gjc/
.claude/settings.local.json

# local toolchains
.tools/

# price warehouse (derived, incrementally refreshed)
data/prices/

.vercel

# agent/run logs
logs/

# secrets - NEVER commit
.env

# stray run logs at root
*.log
